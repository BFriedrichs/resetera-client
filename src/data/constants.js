import { API } from "react-native-dotenv";

export const API_URL = "" + API;
export const BASE_URL = "https://resetera.com";

export const ENDPOINTS = {
  FORUM: "forum",
  THREAD: "thread",
  PUSH: "push-token"
};
